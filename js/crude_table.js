/* crude_table.js — Phase 3 placeholder */

function initCrudeTable(crudeData) {
  // Implemented in Phase 3
  const container = document.getElementById("crude-table-container");
  if (container) {
    container.innerHTML = `<p class="loading-msg" style="color:var(--text-dim);font-size:0.8rem">
      Crude oil table — coming in Phase 3
    </p>`;
  }
}
